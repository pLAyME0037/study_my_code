#include <iostream>

int main()
{
    int apple=128;
    int remainder=9;

    apple/= 2; // + - * / %

    std::cout<<"Result: "<<apple<<'\n';
    std::cout<<"The remain value is: "<<remainder % 2;

    return 0;
}