CREATE DATABASE IF NOT EXISTS clothes_app;
USE clothes_app;

CREATE TABLE users_table (
    user_id       INT AUTO_INCREMENT PRIMARY KEY,
    user_name     VARCHAR(255) NOT NULL,
    user_email    VARCHAR(255) NOT NULL UNIQUE,
    user_password VARCHAR(255) NOT NULL
);

CREATE TABLE items_table (
    item_id     INT AUTO_INCREMENT PRIMARY KEY,
    name        VARCHAR(255) NOT NULL,
    rating      DECIMAL(3,2) DEFAULT 0,
    tags        TEXT,
    price       DECIMAL(10,2) NOT NULL,
    sizes       TEXT,
    colors      TEXT,
    description TEXT,
    image       VARCHAR(255)
);

CREATE TABLE cart_table (
    cart_id   INT AUTO_INCREMENT PRIMARY KEY,
    user_id   INT NOT NULL,
    item_id   INT NOT NULL,
    quantity  INT NOT NULL DEFAULT 1,
    color     VARCHAR(100),
    size      VARCHAR(100),
    FOREIGN KEY (user_id) REFERENCES users_table(user_id),
    FOREIGN KEY (item_id) REFERENCES items_table(item_id)
);

CREATE TABLE favorite_table (
    user_id INT NOT NULL,
    item_id INT NOT NULL,
    PRIMARY KEY (user_id, item_id),
    FOREIGN KEY (user_id) REFERENCES users_table(user_id),
    FOREIGN KEY (item_id) REFERENCES items_table(item_id)
);

CREATE TABLE orders_table (
    order_id        INT AUTO_INCREMENT PRIMARY KEY,
    user_id         INT NOT NULL,
    selectedItems   TEXT NOT NULL,
    deliverySystem  VARCHAR(100),
    paymentSystem   VARCHAR(100),
    note            TEXT,
    totalAmount     DECIMAL(10,2) NOT NULL,
    image           VARCHAR(255),
    status          VARCHAR(50) NOT NULL DEFAULT 'new',
    shipmentAddress TEXT,
    phoneNumber     VARCHAR(50),
    dateTime        TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users_table(user_id)
);

CREATE TABLE admins_table (
    admin_id       INT AUTO_INCREMENT PRIMARY KEY,
    admin_email    VARCHAR(255) NOT NULL UNIQUE,
    admin_password VARCHAR(255) NOT NULL
);

-- Insert default admin (password is MD5 of "admin123")
INSERT INTO admins_table (admin_email, admin_password)
VALUES ('admin@admin.com', MD5('admin123'));

-- Set API permission for image uploads
sudo chown -R http:http api/api_clothes_store/transactions_proof_images/
sudo chmod 755 api/api_clothes_store/transactions_proof_images/

-- Quick check
sudo systemctl status mariadb
