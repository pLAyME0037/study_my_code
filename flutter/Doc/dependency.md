1. animated_text_kit: ^4.3.0
  * https://pub.dev/packages/animated_text_kit
A flutter package which contains a collection of some cool and awesome text
animations.

2. image_picker: ^1.2.2
  * https://pub.dev/packages/image_picker
A Flutter plugin for picking images from the image library, and taking new
pictures with the camera.

3. firebase_auth: ^6.4.0
  * https://pub.dev/packages/firebase_auth
Firebase Authentication provides backend services, easy-to-use SDKs, and
ready-made UI libraries to authenticate users to your app. It supports
authentication using passwords, phone numbers, popular federated identity
providers like Google, Facebook and Twitter, and more.

4. cloud_firestore: ^6.3.0
  * https://pub.dev/packages/cloud_firestore
Flutter plugin for Cloud Firestore, a cloud-hosted, noSQL database with live
synchronization and offline support on Android and iOS.

5. firebase_storage: ^13.3.0
  * https://pub.dev/packages/firebase_storage
This is the name of a package or plugin. Like a pre-made toolbox that lets your
app easily save and retrieve files (like images, videos, or documents) to Google
Firebase Storage, which is a cloud service for storing data. Instead of writing
complex code from scratch to handle file uploads, developers just add this tool
to their project.

6. firebase_core: ^4.7.0
  * https://pub.dev/packages/firebase_core
A Flutter plugin to use the Firebase Core API, which enables connecting to
multiple Firebase apps.

7. uuid: ^4.5.3
  * https://pub.dev/packages/uuid
  * https://www.ietf.org/rfc/rfc4122.txt
  * https://www.rfc-editor.org/rfc/rfc9562.html
Simple, fast generation of RFC4122 and RFC9562 UUIDs.

Features:
    - Generate RFC4122 version 1, version 4, or version 5 UUIDs
    - Supports RFC9562 version 6, version 7, and version 8
    - Runs in web, server, and flutter
    - Cryptographically strong random number generation on all platforms

8. staggered_grid_view: ^0.0.1
  * https://pub.dev/packages/staggered_grid_view
A flexible and performant Flutter package for creating staggered grid layouts,
offering various configuration options for both fixed and dynamic content.

pub package License: BSD

Features: 
    - Staggered Layout: Create visually appealing layouts with variable-sized
      items spanning multiple rows and columns.
    - Fixed and Dynamic Extents: Support for both fixed-size grids and grids that
      adapt to the size of the content.
    - Customizable Delegates: Configure the layout with various delegates for
      cross-axis count, max-extent, and custom tile sizes.
    - Lazy Loading: Efficiently handle large datasets with lazy child creation
      and caching.
    - Automatic Keep-Alives: Option to keep widgets alive for smoother
      transitions during scrolling.
    - Simple Usage: Easy-to-use API similar to Flutter's built-in
      GridView.

9. flutter_swiper_null_safety: ^1.0.2
  * https://pub.dev/packages/flutter_swiper_null_safety
This is a specific, stable version of a plugin that allows Flutter developers to
add sliding image or content carousels to their apps while ensuring the code is
modern and crash-resistant thanks to Dart's null safety features.

10. just_audio: ^0.10.5
  * https://pub.dev/packages/just_audio
A feature-rich audio player for Flutter. Loop, clip and sequence any sound from
any source (asset/file/URL/stream) in gapless playlists.

11. record: ^6.2.0
  * https://pub.dev/packages/record
Audio recorder from microphone to a given file path or stream.

No external dependencies:

    - On Android, AudioRecord and MediaCodec or MediaRecorder.
    - On iOS and macOS, AVFoundation.
    - On Windows, MediaFoundation.
    - On web browser! (and its underlying platform).
    External dependencies:
    - On linux, encoding is provided by parecord, pactl and ffmpeg. It must be
      installed separately.

12. path_provider: ^2.1.5
  * https://pub.dev/packages/path_provider
Flutter plugin for getting commonly used locations on host platform file systems,
such as the temp and app data directories.
