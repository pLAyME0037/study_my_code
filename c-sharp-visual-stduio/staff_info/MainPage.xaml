<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="staff_info.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label Text="Staff Id" />
            <Entry Text="{Binding StaffId}" />

            <Label Text="Name" />
            <Entry Text="{Binding Name}" />

            <Label Text="salary" />
            <Entry Text="{Binding Salary}" />

            <Label Text="Department" />
            <Entry Text="{Binding Department}" />

            <Button Text="Add 100 to Salary"
                    Command="{Binding increaseStaffSalaryCommand}" />

            <Button Text="Load"
                    Command="{Binding loadStaffCommand}" />

            <Button Text="Clear"
                    Command="{Binding clearStaffCommand}" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
