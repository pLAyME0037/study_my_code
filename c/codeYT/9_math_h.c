#include <stdio.h>
#include <math.h>
int main(){

    double A = sqrt(9);// double - %lf
    double B = pow(7,3);
    int C = round(6.1323);// int - %d
    int D = ceil(3.243);
    double E = floor(6.999);
    double F = fabs(-777);
    double G = log(45);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("%lf", J);

    return 0;

}